Create a full featured minimal web application to be hosted on my local network. The application should run in a docker container along the other apps on this server. This application will provide an interface for members of the local network to submit media requests which will be stored in a csv file that is accessible to other apps on this server. This apps request page should also display past requests and their 'status'. Status can be [requested, in progress, fulfilled, not found]. If possible make this application which is accessible only on my local network have a domain name without needing to purchase a domain on the internet (because its local only). The domain should be http://torrentreq.bug
Use what ever tech stack gives the lowest overhead for performance, as this is running on a low power server. 
